{
  "name": "api-gateway",
  "description": "Express Gateway Instance Bootstraped from Command Line",
  "repository": {},
  "license": "UNLICENSED",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "eg gateway start",
    "stop": "eg gateway stop",
    "dev": "eg gateway start --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express-gateway": "^1.16.11",
    "jsonwebtoken": "^9.0.2"
  }
}
